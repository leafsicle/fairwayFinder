<%= form_with(model: [:admin, @user, @scorecard]) do |form| %>
  <% if scorecard.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(scorecard.errors.count, "error") %> prohibited this scorecard from being saved:</h2>
      <ul>
        <% scorecard.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
    <%# TODO: need to add migrations and determine how to flow the routing for creating scorecards %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
